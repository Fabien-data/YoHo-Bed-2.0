# Owner/superuser connection — used for migrations & seeds (bypasses RLS).
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/yohobed

# Restricted role the running app connects as (RLS is enforced). Created by src/rls.sql.
APP_DATABASE_URL=postgresql://yoho_app:yoho_app_pw@localhost:5433/yohobed
