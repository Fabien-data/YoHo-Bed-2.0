NEXT_PUBLIC_API_URL=http://localhost:3001
# Enable the Property profile map preview with a Google Maps Embed API key
# restricted to this site's HTTP referrers. The Google Maps link works without it.
NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY=
