# The API connects as the restricted yoho_app role so RLS is enforced.
APP_DATABASE_URL=postgresql://yoho_app:yoho_app_pw@localhost:5433/yohobed
PORT=3001
JWT_SECRET=dev-only-change-me-in-production
JWT_EXPIRES_IN=1d
