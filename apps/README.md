# apps/

Deployable applications. Scaffolded per phase:

- `api` — NestJS modular monolith (Phase 1+)
- `web-extranet` — Next.js property-owner PMS (Phase 3+)
- `web-staff` — Next.js minimal staff console (Phase 8)
- `worker` — BullMQ workers for CM push & notifications (Phase 6)
- `etl` — MySQL → Postgres migration + parity harness (Phase 10)
